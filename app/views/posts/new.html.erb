<div class="span12">
	<%= simple_form_for [@group,@post] do |f| %>
	<%= f.input :content, :input_html => { :class => "input-xxlarge"} %>
	<div class="form-actions">
		<%= f.submit "Submit", :disable_with => 'Submiting...', :class => "btn bt\
			n-primary" %>
	</div>
	<% end %>
 </div>
